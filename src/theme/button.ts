import type { NuxtOptions } from '@nuxt/schema'

export default (options: Required<NuxtOptions['ui']>) => ({
  slots: {
  },
  variants: {
  },
  compoundVariants: [],
  defaultVariants: {
    color: 'primary'
  }
})
